# PostgreSQL (flower_shop).
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=flower_shop
POSTGRES_USER=postgres
# Must match the running database (compose default is postgres).
POSTGRES_PASSWORD=postgres
POSTGRES_SSL_MODE=disable

GRPC_PORT=50053
# Also serves GET /live and GET /ready for health checks.
HTTP_PORT=8083

BROKERS=127.0.0.1:9092

INVENTORY_SERVICE_HOST=localhost
INVENTORY_SERVICE_PORT=50052
